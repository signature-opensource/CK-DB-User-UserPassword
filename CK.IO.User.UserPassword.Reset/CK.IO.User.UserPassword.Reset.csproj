<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <Description>Extends CK.IO.User.UserPassword with the temporary password support: adds IsTemporary to the SetPassword command and IsTemporaryPassword to the user profile.</Description>
  </PropertyGroup>
  <ItemGroup>
    <PackageReference Include="CK.IO.Actor" Version="0.43.2" />
  </ItemGroup>
  <ItemGroup>
    <ProjectReference Include="..\CK.IO.User.UserPassword\CK.IO.User.UserPassword.csproj" />
  </ItemGroup>
</Project>
